{
  "identifier": "default",
  "description": "Default capabilities for the frontend",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "plugin:updater:default",
    "plugin:process:allow-restart"
  ]
}
